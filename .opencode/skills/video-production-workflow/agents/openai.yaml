interface:
  display_name: "短影音工作流"
  short_description: "分類短影音需求、選擇 preset，並輸出可執行的剪輯策略與 QA 檢查。"
  default_prompt: "請使用 $video-production-workflow 先分類短影音需求、選擇 preset，並在開始剪輯前先產出可執行的剪輯策略。"

policy:
  allow_implicit_invocation: true
