# ANTIGRAVITY SOP
- 以繁體中文回答
- **核心安全守則**：除非用戶明講，不可以刪除原本已建立的任何檔案、不可以做破壞性操作（為執行任務所產生的暫存檔除外，待任務完成，就應該刪除）。

## 🟢 開工 / 我來了
- 確認當前目錄是否在 Git 倉庫
- 優先檢查並讀取根目錄的 `handoff.md`（包含：上次做到哪、目前狀態、下一步與注意事項）
- 比對 `handoff.md` 中的最後更新電腦/更新者與當前系統環境（當前電腦名稱，PowerShell 中可使用 `$env:COMPUTERNAME`）。若更新電腦與當前電腦不同，提示使用者：「⚠️ 上次在另一台電腦 [電腦名稱] 收工，請確認 Google Drive / Git 已完成同步後再繼續工作」。
- 讀取 Obsidian `每日筆記/` 中的「上次做到哪」與「下一步計畫」作為輔助背景脈絡。
- 執行 `git status` 與 `git fetch origin`，檢查本地與遠端分支同步狀況。若有落後（behind），提示使用者手動執行 `git pull`（不主動執行 pull 以免覆蓋本地修改）。
- 給出今日第一步行動建議，並詢問使用者從哪裡開始。

## 🔴 收工 / 下班了
- 執行專案敏感檔案安全掃描（防止 API Key 外洩）
- 彙整今日進度，全新寫入（覆蓋）根目錄的 `handoff.md`：
  - **目前做到哪**：本次完成的具體進度。
  - **目前狀態**：是否可運行？哪些做一半？有無卡關？
  - **下一步**：下次接手最優先執行的 1-3 項。
  - **最後更新**：日期時間、更新者（例如 `Antigravity @ COMPUTERNAME`，使用當前電腦名）、Git push 狀態。
- 更新 `README.md`，寫清楚：專案用途、目前功能、啟動方式、部署方式、環境變數、已知問題、下一步。
- 更新 `docs/PROGRESS.md`，以日期記錄：今天完成什麼、改了哪些重要檔案、做了哪些決策、目前卡在哪裡、下次接手要先看什麼。
- 更新 Obsidian 每日筆記的「已完成工作」與「待辦事項」。
- 執行 `git add` 所有更新、`git commit` 與 `git push`。commit message 必須清楚描述本次改動內容與細節，拒絕簡短無語意的 "update"，且應附上 handoff 的簡短摘要。

## 🔵 初始化專案
- 在根目錄生成 `agents.md`、`ANTIGRAVITY.md`、`handoff.md`（初始交接範本）、`.gitignore` 與首頁說明檔案
- 執行 `git init`，加入所有檔案並完成 initial commit
- 使用 `gh repo create` 建立遠端 GitHub 倉庫並推上去
- 在 Obsidian 第二大腦中建立對應的專案工作區資料夾

> 以上三個關鍵字（「開工」「收工」「初始化專案」）在對話中說出時，AI 助理會自動觸發相應工作流。

## ⚠️ 主動型 ETF 更新安全規範
- **核心資產保護**：`wiki/金融投資/主動型ETF持股明細.xlsx` 存有歷次更新的完整日期分頁與歷史比對資料。
- **嚴禁行為**：
  1. **禁止**直接複製/覆蓋 `wiki/金融投資/` 下的檔案。
  2. **禁止**在 `skills/active-etf-holdings` 本地執行不帶參數的腳本，這樣會生成一個全新、無歷史分頁的空 XLSX 檔並把歷史覆蓋。
- **標準執行流程**：
  - 務必在**知識庫根目錄**執行，且指定 `--output-dir wiki/金融投資` 參數（現行腳本已改寫為優先自動尋找此路徑）。
  - 這會使腳本讀取現有歷史 Excel，在保留所有舊日期分頁的前提下，無縫追加今日分頁並完成增量歷史比對（新增、刪除、加碼、減碼四大歷史異動分頁自動累加）。
